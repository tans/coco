import defaultClient, { request } from "client"

class BaseService
  fn init client
    @client = client

export class UserService extends BaseService
  fn init client
    @client = client

  fn find id
    return request "/users/{id}"

service = new UserService defaultClient
export { service }
export default UserService
